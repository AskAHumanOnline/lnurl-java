spring:
  application:
    name: lnurl-java-examples

server:
  port: 8090

lnurl:
  auth:
    base-url: http://localhost:8090  # WARNING: Change to https:// in production — http:// is for local development only
    challenge-expiry-seconds: 300
  pay:
    fail-on-resolution-error: false
  lnd:
    host: localhost
    rest-port: 10009
    macaroon-path: ""
    tls-cert-path: ""
